import type { NextPage } from "next";
import styles from "../styles/Home.module.sass";

const Home: NextPage = () => {
    

    return (
        <div>
                      
        </div>
    );
};

export default Home;
